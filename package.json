{
  "name": "@f/img-to-canvas",
  "description": "Convert an img tag to a canvas element where it can be manipulated",
  "repository": "git://github.com/micro-js/img-to-canvas.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "hihat": "^2.6.4",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.2.2"
  }
}
